name: G3 clothes washer PDP label EPA comparison candidates

on:
  workflow_dispatch:
    inputs:
      review_run_id:
        description: Successful Washer raw candidate review run ID (blank uses latest success)
        required: false
        type: string
  workflow_run:
    workflows: [G3 clothes washer EnergyGuide raw candidate review]
    types: [completed]
    branches: [main]

permissions:
  actions: read
  contents: read

concurrency:
  group: g3-washer-source-comparison-${{ github.ref }}
  cancel-in-progress: false

jobs:
  compare-candidates:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      GH_TOKEN: ${{ github.token }}
      REVIEW_RUN_ID: ${{ github.event.workflow_run.id || github.event.inputs.review_run_id || '' }}
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version-file: '.python-version'
      - name: Download same-run PDP, label, and EPA source artifacts
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "$REVIEW_RUN_ID" ]; then
            REVIEW_RUN_ID="$(gh api "repos/${GITHUB_REPOSITORY}/actions/workflows/g3-washer-energyguide-review-queue.yml/runs?branch=main&status=success&per_page=1" --jq '.workflow_runs[0].id')"
          fi
          REVIEW_VALID="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${REVIEW_RUN_ID}" --jq 'select(.status == "completed" and .conclusion == "success" and .path == ".github/workflows/g3-washer-energyguide-review-queue.yml") | .id')"
          test "$REVIEW_VALID" = "$REVIEW_RUN_ID"
          mkdir -p runtime/g3-washer/review
          REVIEW_ARTIFACT="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${REVIEW_RUN_ID}/artifacts" --jq '[.artifacts[] | select(.expired == false) | select(.name | startswith("g3-washer-energyguide-review-"))] | sort_by(.created_at) | last | .id')"
          test -n "$REVIEW_ARTIFACT"
          gh api "repos/${GITHUB_REPOSITORY}/actions/artifacts/${REVIEW_ARTIFACT}/zip" > runtime/g3-washer/review.zip
          unzip -q runtime/g3-washer/review.zip -d runtime/g3-washer/review
          REVIEW_FILE="$(find runtime/g3-washer/review -name review-queue.json -type f -print -quit)"
          test -n "$REVIEW_FILE"
          REVIEW_DIR="$(dirname "$REVIEW_FILE")"
          COLLECTION_RUN_ID="$(python -c 'import json,sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["collection_run_id"])' "$REVIEW_FILE")"
          SECONDARY_RUN_ID="$(python -c 'import json,sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["retrieval_run_id"])' "$REVIEW_FILE")"
          test -n "$COLLECTION_RUN_ID"
          test -n "$SECONDARY_RUN_ID"

          COLLECTION_VALID="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${COLLECTION_RUN_ID}" --jq 'select(.status == "completed" and .conclusion == "success" and .path == ".github/workflows/g3-washer-collection.yml") | .id')"
          test "$COLLECTION_VALID" = "$COLLECTION_RUN_ID"
          COLLECTION_ARTIFACT="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${COLLECTION_RUN_ID}/artifacts" --jq '[.artifacts[] | select(.expired == false) | select(.name | startswith("g3-washer-collection-"))] | sort_by(.created_at) | last | .id')"
          test -n "$COLLECTION_ARTIFACT"
          mkdir -p runtime/g3-washer/collection
          gh api "repos/${GITHUB_REPOSITORY}/actions/artifacts/${COLLECTION_ARTIFACT}/zip" > runtime/g3-washer/collection.zip
          unzip -q runtime/g3-washer/collection.zip -d runtime/g3-washer/collection
          COLLECTION_FILE="$(find runtime/g3-washer/collection -name collection-summary.json -type f -print -quit)"
          test -n "$COLLECTION_FILE"

          SECONDARY_VALID="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${SECONDARY_RUN_ID}" --jq 'select(.status == "completed" and .conclusion == "success" and .path == ".github/workflows/g3-washer-secondary-sources.yml") | .id')"
          test "$SECONDARY_VALID" = "$SECONDARY_RUN_ID"
          EPA_ARTIFACT="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${SECONDARY_RUN_ID}/artifacts" --jq '[.artifacts[] | select(.expired == false) | select(.name | startswith("g3-washer-epa-"))] | sort_by(.created_at) | last | .id')"
          LABEL_ARTIFACT="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${SECONDARY_RUN_ID}/artifacts" --jq '[.artifacts[] | select(.expired == false) | select(.name | startswith("g3-washer-energyguide-"))] | sort_by(.created_at) | last | .id')"
          test -n "$EPA_ARTIFACT"
          test -n "$LABEL_ARTIFACT"
          mkdir -p runtime/g3-washer/epa runtime/g3-washer/retrieval
          gh api "repos/${GITHUB_REPOSITORY}/actions/artifacts/${EPA_ARTIFACT}/zip" > runtime/g3-washer/epa.zip
          gh api "repos/${GITHUB_REPOSITORY}/actions/artifacts/${LABEL_ARTIFACT}/zip" > runtime/g3-washer/label.zip
          unzip -q runtime/g3-washer/epa.zip -d runtime/g3-washer/epa
          unzip -q runtime/g3-washer/label.zip -d runtime/g3-washer/retrieval
          EPA_FILE="$(find runtime/g3-washer/epa -name capture-summary.json -type f -print -quit)"
          test -n "$EPA_FILE"
          echo "REVIEW_DIR=${REVIEW_DIR}" >> "$GITHUB_ENV"
          echo "COLLECTION_DIR=$(dirname "$COLLECTION_FILE")" >> "$GITHUB_ENV"
          echo "COLLECTION_RUN_ID=${COLLECTION_RUN_ID}" >> "$GITHUB_ENV"
          echo "EPA_DIR=$(dirname "$EPA_FILE")" >> "$GITHUB_ENV"
          echo "EPA_RUN_ID=${SECONDARY_RUN_ID}" >> "$GITHUB_ENV"
      - name: Build candidate comparison without assessment
        run: >-
          python scripts/g3_washer_source_comparison.py
          --review-root "$REVIEW_DIR"
          --collection-root "$COLLECTION_DIR"
          --collection-run-id "$COLLECTION_RUN_ID"
          --epa-root "$EPA_DIR"
          --epa-run-id "$EPA_RUN_ID"
          --out runtime/g3-washer/comparison
      - name: Preserve the source comparison
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: g3-washer-source-comparison-${{ github.run_id }}-${{ github.run_attempt }}
          path: runtime/g3-washer/comparison/
          retention-days: 14
          if-no-files-found: warn
