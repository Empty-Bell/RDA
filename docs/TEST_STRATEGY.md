# Phase 0 validation

Current G0 result: PASS for reconnaissance/hosted foundation; see
G0_ACCEPTANCE_RECORD.md. Combined hosted checkpoint 35169843199 replayed 214
contracts (one optional CV test skipped; executed in both prior installed cold
jobs) and verified 99 fixture references. Required category coverage, existing
live probes and policy deferrals are documented there.

Hosted runtime subgate: clean dependency install, 3 guardrail tests, headless Chromium,
synthetic embedded PDF extraction and image-only RapidOCR fallback, disk budget,
live Samsung network observations and ENERGY STAR catalog access.

G0 requires all 11-family source contracts, pagination/count grain,
exact SKU/PDP identity, live EnergyGuide provenance/download, EPA dataset semantics,
sanitized fixtures and fixture-backed parser contracts verified on hosted Ubuntu.
These source observation requirements are accepted; assessment/matching rules
and the complete production populations remain later-phase gates.

Do not infer source absence from a cloud runner 403/429 or parser failure.
See EXECUTION_GUIDE G0–G7 for subsequent acceptance criteria.
